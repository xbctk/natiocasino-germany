# Техническое задание: National Casino Germany

## 1. Общая информация

### 1.1. Тип проекта

Обзорный партнёрский сайт бренда казино и беттинга National Casino для аудитории из Новой Зеландии.

### 1.2. Цель сайта

Презентовать бренд National Casino и в понятной форме рассказать пользователю:

- какие услуги и функции доступны на платформе;
- как зарегистрироваться и пользоваться сайтом;
- какие бонусы и промоакции предлагает бренд;
- как установить и использовать мобильное приложение;
- какие преимущества и недостатки есть у платформы;
- какие возможности казино и беттинга представлены на сайте бренда.

### 1.3. Целевая аудитория

- игроки в онлайн-казино;
- игроки в онлайн-игровые аппараты и слоты;
- беттинг-игроки, интересующиеся ставками на спорт;
- пользователи, интересующиеся азартными играми и изучающие новый бренд;
- пользователи, которые ранее не пользовались казино или беттинг-платформами.

### 1.4. География и язык

- целевой регион: Новая Зеландия;
- язык сайта: английский;
- локализация текста в рамках разработки не требуется.

### 1.5. Оригинальный сайт бренда

Оригинальный сайт National Casino: [https://natiocazino.com/](https://natiocazino.com/).

Оригинальный сайт используется как основной источник фирменного визуального языка:

- логотипа;
- цветовой палитры;
- шрифтов;
- типографических приёмов;
- декоративных элементов;
- оформления кнопок и интерактивных состояний.

На момент подготовки ТЗ оригинальный сайт из текущего региона открывается в виде фирменной региональной страницы ограничения доступа. Цвета и визуальные приёмы дополнительно зафиксированы по предоставленным скриншотам основной версии сайта и desktop-макету `docs/source/1728w default.pdf`.

Приоритет визуальных источников:

1. Для комбинации и композиции контентных блоков, списков, таблиц и карточек — материалы из папки `Референсы`.
2. Для всех остальных решений — явные требования и значения, зафиксированные в этом ТЗ.
3. Desktop-макет `docs/source/1728w default.pdf`, предоставленные скриншоты и доступная версия оригинального сайта используются только как источники уже перенесённых в ТЗ данных и не заменяют требования ТЗ напрямую.

Приоритет папки `Референсы` распространяется только на структуру, взаимное расположение и способы комбинирования блоков, списков, таблиц и карточек. Цвета, шрифты, размеры, отступы, логотип, кнопки, навигация, адаптивность, контент и остальные параметры необходимо брать из этого ТЗ.

Значения из макета, перенесённые в ТЗ, не должны автоматически заменяться значениями с оригинального сайта без отдельного обновления ТЗ.

## 2. Контент

### 2.1. Источники текста

На сайте используется только готовый текст из следующих файлов:

- `Тексты/National Casino Germany Main Page.docx`;
- `Тексты/National Casino Germany Bonuses.docx`;
- `Тексты/National Casino Germany App.docx`.

Отдельный DOCX-источник Responsible Gaming для немецкой версии не предоставлен. По прямому указанию пользователя от 19 августа 2026 года для маршрута `/responsible-gaming` действует зафиксированное исключение: существующая структура из четырёх блоков локализована на немецкий язык, а NZ-ресурсы помощи заменены на актуальные немецкие сведения BIÖG/Check dein Spiel и OASIS. Этот текст не является источником для остальных страниц и не разрешает свободное дополнение редакционного контента.

### 2.2. Правила работы с текстом

- Основной редакционный контент берётся только из DOCX-файлов и никак иначе.
- Не дополнять основной редакционный контент новой информацией.
- Не локализовать и не переводить текст.
- Не проводить фактчекинг и не изменять смысл утверждений.
- Не добавлять новые маркетинговые, юридические или информационные формулировки.
- Интерфейсные подписи, названия пунктов навигации, кнопок, элементов sidebar и другой служебный микротекст добавляются в ТЗ отдельно по мере необходимости.
- Не придумывать отсутствующий интерфейсный микротекст самостоятельно, кроме технических accessibility-атрибутов, которые не отображаются как редакционный контент и необходимы для корректной работы интерфейса.
- Распределение текста по блокам и точная структура каждой страницы будут отдельно зафиксированы в этом ТЗ.

## 3. Структура сайта

### 3.1. Основные страницы

Сайт состоит из трёх основных страниц:

| Страница | Рабочее название | URL |
| --- | --- | --- |
| Главная | Main | `/` |
| Бонусы | Bonuses | `/bonuses` |
| Приложение | App | `/app` |

### 3.2. Навигация

Ссылки на страницы Main, Bonuses и App должны находиться:

- в header сайта;
- в footer сайта.

Дополнительно на каждой основной странице в desktop-версии слева располагается закреплённый sidebar с навигацией по контентным блокам текущей страницы.

Левый sidebar должен:

- находиться у левого края основной области сайта;
- оставаться видимым при вертикальной прокрутке страницы;
- содержать список якорных ссылок на основные секции текущей страницы;
- прокручивать страницу к выбранному блоку;
- визуально выделять пункт, соответствующий текущему видимому блоку страницы;
- учитывать высоту закреплённого header, чтобы заголовок выбранной секции не перекрывался;
- сохранять удобную для чтения ширину основной контентной области;
- иметь отдельный список пунктов для страниц Main, Bonuses и App в соответствии со структурой каждой страницы.

На tablet и mobile закреплённый левый sidebar заменяется компактным меню разделов текущей страницы. Компактное меню должно сохранять доступ ко всем тем же якорным ссылкам.

Header используется для переходов между страницами, а левый sidebar — для навигации внутри текущей страницы.

Точные названия пунктов меню и порядок ссылок будут определены при описании header, footer и структуры каждой страницы.

### 3.3. Системные страницы

На текущем этапе системные и юридические страницы не разрабатываются.

Страница Responsible Gaming локализована для Германии по прямому указанию пользователя и использует маршрут `/responsible-gaming`, общий Header, Hero без CTA-кнопки, Sidebar, Footer и четыре контентных блока. Все видимые заголовки, описания, навигация, карточки, предупреждения и metadata страницы используются только на немецком языке; упоминания New Zealand, Aotearoa, `en_NZ` и новозеландские контакты помощи запрещены. Раздел помощи содержит немецкие ресурсы BIÖG/Check dein Spiel и OASIS; карточки сохраняют текущее оформление, но не являются ссылками и не содержат стрелок перехода. Пункт `VERANTWORTUNGSVOLLES SPIELEN` в footer ведёт на локальный маршрут `/responsible-gaming`. В первом контентном блоке обычный текст занимает всю ширину, а под ним три карточки принципов располагаются в три колонки на desktop. Лицензионный абзац располагается сразу под заголовком секции, а далее идёт фоновая композиция с `public/content/responsible-gaming/license-security.webp`; четыре лицензионные карточки размещаются слева одной колонкой, при этом голова персонажа остаётся полностью видимой. Карточки принципов, лицензии и лимитов используют общий паттерн `info-card`, иконку `public/content-icons/cards.png`, одинаковые размеры текста и центральное выравнивание; иконка и заголовок располагаются в одной строке. Внутренняя геометрия повторяет карточку Blackjack на Main: padding `var(--space-card)`, отступы вокруг разделителя `var(--space-nested)`, иконка высотой `1em`, естественная высота по содержимому, заголовок `20px/1.3`, основной текст `16px/1.65`. Лицензионная фоновая композиция не имеет принудительной высоты: расстояние от первой карточки до верхнего края и от последней карточки до нижнего края одинаково. В блоке `Selbstsperre, Limits & Spielzeiterinnerungen` элементы `24/7-Live-Chat`, `Selbstsperre beantragen` и следующий абзац располагаются сразу под H2, вне фоновой композиции. Фоновая композиция использует изображение `public/content/responsible-gaming/self-exclusion-wide.webp` формата `16:9`: персонаж и основные предметы располагаются справа, а слева сохраняется свободная затемнённая зона для четырёх карточек лимитов. Оба фоновых блока используют стандартный внутренний отступ контентных секций `var(--space-content)` и не имеют принудительной высоты. Текст `Spielzeiterinnerungen` располагается под фоновой композицией без бокового выделения.

Актуализация по прямому указанию пользователя от 19 августа 2026 года: пункт `VERANTWORTUNGSVOLLES SPIELEN` в footer использует общий affiliate URL, как и остальные пункты footer; остальные footer-ссылки не меняются. Значение лицензии на странице Responsible Gaming совпадает с footer: `8048/JAZ2017-067`. На ширинах `375px`, `360px` и `320px` breadcrumb, H1 и описание Hero полностью помещаются в viewport; длинные немецкие слова переносятся без изменения текста.

В footer необходимо предусмотреть отдельную область для будущих ссылок на такие страницы. Состав и названия ссылок будут указаны позднее.

Актуализация по прямому указанию пользователя от 24 августа 2026 года заменяет предыдущие ограничения этого подраздела. Реализованы семь внутренних технических страниц: `/responsible-gaming`, `/terms-and-conditions`, `/privacy-policy`, `/bonus-terms`, `/faq`, `/about-us` и `/contact`. Все страницы используют общий Header и Footer, но не используют Hero, Sidebar, изображения, карточки, CTA, формы, внешние ссылки или дополнительные кнопки. Над H1 сохраняются хлебные крошки `Startseite → текущая страница`. Контент состоит только из H1, вводного абзаца, H2, обычных абзацев и списков с существующим Poppins и системными цветами текста `--text`, `--text-secondary`, `--muted`, `--gold`. Немецкий текст является сжатым пересказом соответствующих страниц оригинальной немецкой локации `https://natiocazino.com/de` без добавления новых заявлений. Страница Contact описывает состав контактного запроса, но не содержит формы или контактных ссылок. Все семь маршрутов включаются в sitemap.

## 4. Структура страниц

Детальная структура, порядок секций, содержимое блоков и привязка готового текста будут описаны отдельно для каждой страницы.

### 4.1. Main

Источник текста: `Тексты/National Casino Germany Main Page.docx`.

Страница состоит из 20 контентных блоков, соответствующих H1 и H2 исходного DOCX. Порядок блоков, H3-подразделов, абзацев, списков и таблиц сохраняется строго по исходному файлу. Hero, header, sidebar и footer в эти 20 блоков не входят.

Немецкая структура Main переносится в существующую дизайн-систему проекта без изменения редакционного текста. Используются уже реализованные паттерны: двухколоночные секции с локальными изображениями, фоновые текстовые композиции, карточки списков и фактов, оформленные таблицы и горизонтальная лента отзывов. Новые изображения и новый визуальный язык для этого этапа не создаются.

#### 4.1.1. Hero Main

- Заголовок из `META TITLE`: `National Casino: Slots, Live-Spiele & echte RTP in Deutschland`.
- Описание из `META DESCRIPTION`: `National Casino legt die Karten offen — Live-RTP auf jeder Kachel, eine thematische Lobby, Evolution-Livetische und ein 15-stufiger VIP-Aufstieg.`
- Над заголовком сохраняется существующая строка рейтинга; хлебные крошки в Hero Main не используются.
- Существующая CTA Hero сохраняется как интерфейсный элемент и не относится к редакционному тексту DOCX.

#### 4.1.2. Структура контентных блоков

1. `National Casino: Ein Spielsaal, der seine Karten offenlegt` — два вступительных абзаца.
2. `Was diesen Spielsaal auszeichnet` — два вводных абзаца, список `Das Gute`, список `Die Kompromisse`.
3. `Das technische Bild auf einen Blick` — вводный абзац, таблица характеристик с заголовками колонок `Feature` и `Details`, два поясняющих абзаца без отдельной подложки.
4. `Die Lobby wie eine Landkarte lesen` — два полноширинных абзаца; после первого абзаца располагается предоставленный пользователем снимок лобби `public/content/main/lobby-map.webp`.
5. `Die Zahlen auf der Kachel` — четыре абзаца о Live-RTP и его интерпретации; после второго абзаца располагается утверждённый снимок ряда игровых карточек `public/content/main/live-rtp-strip.png`.
6. `Wie man eine Spielkachel im National Casino liest` — шесть последовательных полноширинных абзацев без карточной сетки; после третьего абзаца повторяется утверждённый снимок `public/content/main/live-rtp-strip.png`.
7. `Die Slots, mit ihren Zahlen` — вводный абзац, таблица `Spiel / Anbieter / RTP (Standard) / Min. Einsatz / Max. Einsatz / Volatilität / Max. Gewinn`, поясняющий абзац.
8. `Die Anbieter hinter dem Spielsaal` — два абзаца; справа поверх фоновой композиции размещается список популярных провайдеров с фирменными иконками и примерами их игр из текущей выдачи `https://natiocazino.com/casino/recommended`.
9. `Die Live-Etage` — четыре полноширинных абзаца; после второго абзаца располагается ряд из шести оригинальных карточек игровых шоу, после третьего — ряд из восьми оригинальных карточек классических live-столов.
10. `Boni und Aktionen` — полноширинный вводный абзац, H3-подразделы `Ersteinzahlungsbonus`, `Krypto-Ersteinzahlungsbonus`, `Zweite Einzahlungsbonus`, `Highroller-Bonus`, `Freitags-Reload-Bonus`, `VIP-Sonntagsturbo`, `Jenseits der Stammgäste`, оформленные как иллюстрированные промо-баннеры, затем полноширинный заключительный абзац об условиях.
11. `Der VIP-Aufstieg` — два абзаца.
12. `Turniere` — один абзац.
13. `Loslegen: Registrierung und Anmeldung` — два абзаца.
14. `Spielen am Handy` — два абзаца.
15. `Einzahlungen, Auszahlungen und Support` — два абзаца.
16. `Ein Wort zum verantwortungsvollen Spielen` — два абзаца.
17. `Häufig gestellte Fragen` — шесть исходных абзацев оформляются как одноколоночный FAQ-аккордеон: первое предложение каждого абзаца до вопросительного знака является вопросом, оставшийся текст — раскрывающимся ответом; формулировки и порядок не изменяются, первый пункт открыт по умолчанию, одновременно раскрывается не более одного ответа.
18. `Was Spieler tatsächlich sagen` — вводный абзац и горизонтальный слайдер из шести отзывов. В каждой исходной записи первое предложение является заголовком карточки, имя после последнего длинного тире выводится отдельным автором, а текст между ними остаётся телом отзыва без переписывания. Карточки используют композицию референса: центрированная верхняя группа с золотым заголовком и светлым именем автора, золотой разделитель и текст отзыва ниже; рейтинг и страна не добавляются без источника.
19. `Die Bewertungskarte` — на desktop двухколоночная композиция `50/50`: таблица `Kategorie / Bewertung` слева и локальное изображение `public/content/main/support.webp` справа; поясняющий абзац располагается на всю ширину под колонками. Числовая выразительность оценок сохраняется, но текстовые звёзды заменяются локальными иконками `public/icons/fire.svg`; половинные значения показываются отдельной полузакрашенной иконкой огня без символа `½`. На tablet и mobile таблица и изображение перестраиваются в одну колонку.
20. `Das letzte Wort` — два итоговых абзаца и строка авторства.

После редакционного содержимого блоков `Wie man eine Spielkachel im National Casino liest`, `Die Slots, mit ihren Zahlen`, `Die Anbieter hinter dem Spielsaal`, `Die Live-Etage`, `Der VIP-Aufstieg`, `Turniere`, `Loslegen: Registrierung und Anmeldung` и `Das letzte Wort` располагается основной CTA `Jetzt beitreten`. Блоки `Das technische Bild auf einen Blick`, `Die Lobby wie eine Landkarte lesen` и `Die Zahlen auf der Kachel` CTA не содержат. Кнопка ведёт на общую партнёрскую ссылку, использует утверждённый стиль primary CTA и является интерфейсным элементом, не частью редакционного текста DOCX.

#### 4.1.3. Навигация Main

Desktop-sidebar и компактная tablet/mobile-навигация ведут ко всем 20 блокам в указанном порядке. Подписи ссылок совпадают с заголовками соответствующих блоков DOCX.

#### 4.1.4. Полноразмерные секции Main

- Блоки `National Casino: Ein Spielsaal, der seine Karten offenlegt`, `Die Anbieter hinter dem Spielsaal`, `Turniere`, `Loslegen: Registrierung und Anmeldung`, `Spielen am Handy`, `Einzahlungen, Auszahlungen und Support` и `Ein Wort zum verantwortungsvollen Spielen` используют полноразмерную фоновую композицию: локальное тематическое изображение заполняет секцию по ширине и высоте в режиме `cover`, текст располагается слева поверх чёрного gradient overlay.
- В секции `Die Anbieter hinter dem Spielsaal` поверх фоновой композиции справа размещается локальный одноколоночный список из шести популярных провайдеров `Pragmatic Play`, `Dynabit Gaming`, `BGaming`, `Hacksaw`, `Felix Gaming` и `Spinomenal`. Каждая запись содержит официальную локальную иконку провайдера и названия одной–двух игр, подтверждённых текущей выдачей страницы `https://natiocazino.com/casino/recommended`; названия игр выводятся немного крупнее вторичного текста исходной версии списка. На desktop список занимает правую половину секции, вертикально центрируется относительно текстовой колонки и оформляется как полупрозрачная тёмная панель с золотой обводкой и внутренним отступом `20px`; на tablet/mobile переносится под текст и сохраняет одну колонку без горизонтального переполнения.
- Во всех контентных секциях Main внутренний отступ от границы секции до контента составляет `20px`. У последнего содержательного элемента секций `Häufig gestellte Fragen`, `Ein Wort zum verantwortungsvollen Spielen`, `Einzahlungen, Auszahlungen und Support`, `Turniere`, `Was Spieler tatsächlich sagen` и `Die Bewertungskarte` не должно быть дополнительного нижнего внешнего поля, визуально удваивающего этот отступ.
- В секции `Loslegen: Registrierung und Anmeldung` поверх фоновой композиции справа размещается предоставленный пользователем снимок регистрационного интерфейса `public/content/main/registration-signup.webp`. На desktop он занимает правую половину секции и оформляется на тёмной внутренней подложке с золотой обводкой и согласованными скруглениями по паттерну утверждённых вставок Live-RTP; на tablet/mobile располагается под текстом и занимает доступную ширину.
- На desktop заголовки этих семи секций отображаются в одну строку. На tablet/mobile допускается естественный перенос без горизонтального переполнения.
- На tablet/mobile текст занимает доступную ширину, а затемнение усиливается по всей площади секции для сохранения читаемости.

Секция `Die Lobby wie eine Landkarte lesen` является исключением из фонового паттерна: она использует обычную поверхность контентного блока без фонового изображения и overlay, оба абзаца занимают всю доступную ширину. Снимок `public/content/main/lobby-map.webp` размером `3304×1396px` вставляется после первого абзаца, отображается целиком на тёмной внутренней подложке с золотой обводкой и согласованными скруглениями. На узких экранах снимок сохраняет исходные пропорции и доступен внутри собственного горизонтального scroll-контейнера без переполнения страницы.

#### 4.1.5. Секции Live-RTP Main

- Блок `Die Zahlen auf der Kachel` использует обычную поверхность контентной секции без фонового изображения и overlay; все четыре абзаца занимают всю доступную ширину.
- Сразу после второго абзаца вставляется предоставленный пользователем PNG `public/content/main/live-rtp-strip.png` размером `2494×518px`. Изображение отображается целиком без обрезки, разделения на отдельные игровые карточки или замены другим ассетом.
- Блок `Wie man eine Spielkachel im National Casino liest` использует тот же полноширинный текстовый паттерн без карточной сетки. Тот же PNG повторяется сразу после третьего из шести абзацев.
- Обе вставки изображения располагаются на тёмной внутренней подложке с золотой обводкой; подложка и само изображение имеют согласованные скругления.
- На узких экранах изображение сохраняет исходные пропорции и доступно внутри собственного горизонтального scroll-контейнера без горизонтального переполнения страницы.

#### 4.1.6. Секция `Die Live-Etage` Main

- Блок использует обычную поверхность контентной секции без фонового изображения и overlay; все четыре исходных абзаца занимают всю доступную ширину.
- После второго абзаца вставляется ряд оригинальных карточек со страницы National Casino для шести названных игровых шоу: `Crazy Time`, `Funky Time`, `Monopoly Big Baller`, `Crazy Coin Flip`, `Lightning Storm` и `Sweet Bonanza Candyland`.
- После третьего абзаца вставляется ряд оригинальных карточек, представляющих перечисленные классические live-столы: `Aurora Blackjack Libra`, `Speed Blackjack Varna`, `Infinite Free Bet Blackjack`, `Casino Hold'em`, `Texas Hold'em Bonus`, `Gates of Olympus Roulette`, `Baccarat 1` и `Craps`.
- Ассеты сохраняются локально в `public/content/main/live-games/`; внешние URL в runtime не используются. Названия, уже встроенные в оригинальные карточки, не дублируются отдельными подписями.
- Оба ряда располагаются на тёмной внутренней подложке с золотой обводкой и согласованными скруглениями, как утверждённые вставки в секциях Live-RTP.
- На узких экранах карточки сохраняют исходные пропорции и доступны внутри собственного горизонтального scroll-контейнера без горизонтального переполнения страницы.

#### 4.1.7. Секция `Boni und Aktionen` Main

- Вводный и заключительный абзацы занимают всю доступную ширину секции.
- Семь исходных H3-подразделов сохраняют порядок и редакционный текст, но визуально оформляются как промо-баннеры по композиционному паттерну оригинальной страницы National Casino Promotions: тематический фон заполняет карточку, отдельный прозрачный персонаж располагается справа, текст — слева поверх затемняющего gradient overlay.
- Для первых шести баннеров используются соответствующие оригинальные пары фона и персонажа: welcome bonus, crypto bonus, second deposit, highroller, Friday reload и VIP Sunday. Для объединённого подраздела `Jenseits der Stammgäste` используется оригинальная композиция Secret Bonus как прямое соответствие первому упомянутому предложению.
- В каждой карточке связка заголовка и текста получает локальную тёмную подложку с прозрачностью около 30%. Текстовая область широкого баннера `Jenseits der Stammgäste` занимает 83,78% внутренней ширины карточки, доходя до визуальной зоны персонажа без перекрытия текста.
- Перед заголовком каждой из семи промо-карточек располагается декоративная локальная иконка огня `public/icons/fire.svg` размером `20×20px`; иконка выравнивается с заголовком и скрыта от accessibility tree как CSS-декор. Заголовки отделяются от текста тонкой золотой нижней границей; внутренний отступ до границы и внешний отступ до абзаца равны системному `var(--space-nested)`. Абзацы внутри карточек не используют глобальное ограничение `85ch` и занимают всю ширину своей текстовой подложки.
- После текста внутри каждой из семи карточек располагается основная CTA-ссылка `Bonus Holen` с системным отступом `var(--space-content)`. CTA ведёт на бонусный affiliate URL `https://media.toxtren.com/redirect.aspx?pid=159513&bid=2036&lpid=2084`; поверхность карточки остаётся некликабельной.
- Все оригинальные ассеты сохраняются локально в `public/content/main/bonuses/`; внешние URL в runtime не используются.
- На desktop первые шесть баннеров располагаются в две колонки, а `Jenseits der Stammgäste` занимает обе колонки. На tablet/mobile карточки перестраиваются в одну колонку, текст сохраняет полную читаемость, а фон и персонаж остаются декоративными и не создают горизонтального переполнения.

### 4.2. Bonuses

Источник текста: `Тексты/National Casino Germany Bonuses.docx`.

Страница состоит из 12 контентных блоков, соответствующих Heading 1 и всем Heading 2 исходного DOCX. Порядок блоков, вложенность подразделов Heading 3, последовательность абзацев и двух нумерованных списков сохраняются строго по исходному файлу. Hero, общие Header, Sidebar и Footer в эти 12 блоков не входят.

#### 4.2.1. Hero Bonuses

- Сохраняется общий компонент и утверждённая композиция Hero.
- Заголовок берётся из `META TITLE`: `National Casino Boni in Deutschland: Willkommen, Spins & VIP`.
- Описание берётся из `META DESCRIPTION`: `Jeder Bonus im National Casino auf einen Blick — 200 % Krypto-Willkommen, Freitags-Reloads, 1-€-Mega-Spins, ein 15-stufiger VIP-Aufstieg und Treasure-Race-Turniere.`
- Над заголовком отображаются хлебные крошки `Startseite → Boni`.
- Основная CTA-ссылка Hero использует общую подпись `Jetzt beitreten`.
- Hero использует локальный фон `public/content/bonuses/hero-characters-v2.webp` формата `16:9`: мужчина и женщина из утверждённых референсов расположены вместе справа и держат монеты/фишки; слева сохраняется тёмная сейф-зона под текст и существующий overlay.

#### 4.2.2. Структура контентных блоков

1. `National Casino Boni & Aktionen in Deutschland` — вводный обзор бонусного календаря.
2. `Warum sich die Aktionen hier für deutsche Spieler lohnen` — два последовательных абзаца о разнообразии предложений и выборе подходящего бонуса.
3. `Das Willkommenspaket, auf zwei Arten`:
   - `Was du tatsächlich bekommst` — вводная строка и последовательные описания Standard-Ersteinzahlungsbonus, Krypto-Ersteinzahlungsbonus и Zweiter Einzahlungsbonus;
   - `So holst du ihn dir` — вводная строка и нумерованный список из 5 шагов;
   - `Die Bedingungen, die man kennen sollte` — заключительный абзац об Umsatzanforderung, сроке действия и подходящих играх.
4. `Reload-Boni`:
   - вводный абзац;
   - `Der Freitags-Reload`;
   - `Die Alltags-Reloads`.
5. `Freispiele über den ganzen Kalender`:
   - вводный абзац;
   - `Woher die Spins kommen`;
   - `Die Spins, die tatsächlich mehr wert sind`.
6. `Treuebelohnungen & VIP-Vorteile`:
   - вводный абзац;
   - `Wie du verdienst`;
   - `Die Stufen und was sie wert sind` и два последовательных абзаца.
7. `Mehr Wege, auf denen der Wert zurückkommt` — два последовательных абзаца о Glücksrad, Sonntags-Mega-Spins, Prize Drops, Pragmatic Play Drops & Wins и BGaming Drops.
8. `Turniere und Bestenlisten`:
   - вводный абзац;
   - `Was läuft`;
   - `Wie du mitmachst`.
9. `Aktionscodes: Dein Ticket für etwas Extra` — вводный абзац, вводная строка, нумерованный список из 4 шагов и заключительный абзац.
10. `Zeitlich begrenzte und saisonale Angebote` — три последовательных абзаца о Geheimbonus, сезонных акциях и ограниченных по времени сетевых событиях.
11. `Verantwortungsvoll spielen` — два последовательных абзаца об игровых лимитах, самоисключении, криптовалютных депозитах и организациях помощи.
12. `Dein Angebot wählen` — итоговый абзац и CTA `Bonus Holen`.

#### 4.2.3. Навигация Bonuses

Sidebar и его компактная tablet/mobile-версия ведут ко всем 12 контентным блокам в том же порядке и используют их заголовки без создания новых редакционных названий.

#### 4.2.4. Композиция Bonuses

- Общие Header, Hero, Sidebar, Footer, поверхности секций, типографика, кнопки и нумерованные инструкции используют дизайн-систему и компонентные паттерны Main.
- Во всех 12 контентных секциях Bonuses расстояние от внутренней границы секции до ближайшего содержимого составляет `20px` со всех сторон. Последний абзац, сетка, список или подраздел не сохраняет дополнительный нижний `margin`, визуально удваивающий этот отступ. Фоновые текстовые секции определяют высоту по содержимому и не создают пустую нижнюю зону за счёт фиксированной `min-height`.
- Прямые абзацы секций Bonuses без фоновой композиции занимают всю доступную ширину между боковыми отступами `20px`; ограничение ширины текста сохраняется только в фоновых композициях с изображением справа.
- В первом блоке, блоке `Warum sich die Aktionen hier für deutsche Spieler lohnen`, VIP, турнирах, сезонных предложениях, ответственном игровом поведении и итоговом блоке используются новые полноширинные фоновые композиции `16:9` с затемнением и текстом слева. Персонажи и смысловые объекты располагаются справа, слева сохраняется сейф-зона. Сквозное чередование с учётом иллюстрации `Aktionscodes` идёт так: женщина (`intro-calendar-v2.webp`), мужчина (`promotions-choice-v2.webp`), женщина (`vip-rewards-v2.webp`), мужчина (`tournaments-v2.webp`), женщина (`promotion-code-v3.webp`), мужчина (`seasonal-offers-v2.webp`), женщина (`responsible-play-v2.webp`), мужчина (`offer-choice-v2.webp`). Все файлы хранятся в `public/content/bonuses/`.
- Описания конкретных welcome-, reload- и free-spins-бонусов оформляются карточками по паттерну `Boni und Aktionen` на Main: локальный фон, отдельный персонаж справа, затемняющая подложка и текст слева. Карточки не являются ссылками.
- `Das Willkommenspaket, auf zwei Arten` использует три карточки: Standard-Ersteinzahlungsbonus, Krypto-Ersteinzahlungsbonus и Zweiter Einzahlungsbonus. Первые две образуют две колонки на desktop, третья занимает полную ширину; на tablet/mobile все карточки переходят в одну колонку.
- `Reload-Boni` и `Freispiele über den ganzen Kalender` используют по две карточки в desktop-композиции 50/50 и одну колонку на tablet/mobile.
- Каждая из пяти бонусных карточек в блоках `Das Willkommenspaket, auf zwei Arten` и `Reload-Boni` содержит после текста CTA-ссылку `Bonus Holen`, ведущую на бонусный affiliate URL. Две карточки блока `Freispiele über den ganzen Kalender` CTA не содержат. Кликабельной остаётся только CTA, а не поверхность карточки.
- После редакционного содержимого блоков `Das Willkommenspaket, auf zwei Arten`, `Treuebelohnungen & VIP-Vorteile` и `Turniere und Bestenlisten` располагается секционная CTA `Jetzt beitreten`, ведущая на общий affiliate URL. Блоки `National Casino Boni & Aktionen in Deutschland`, `Mehr Wege, auf denen der Wert zurückkommt`, `Aktionscodes: Dein Ticket für etwas Extra` и `Zeitlich begrenzte und saisonale Angebote` секционной CTA не содержат.
- Блок `Warum sich die Aktionen hier für deutsche Spieler lohnen` использует секционную CTA `Bonus Holen` с бонусным affiliate URL. Блоки `Reload-Boni`, `Freispiele über den ganzen Kalender` и `Verantwortungsvoll spielen` не получают секционную CTA; CTA внутри бонусных карточек сохраняются только в блоке Reload.
- Нумерованный список `So holst du ihn dir` располагается слева от существующего изображения регистрации `public/content/main/registration-signup.webp`. Список `Aktionscodes` располагается слева от иллюстрации `public/content/bonuses/promotion-code-v3.webp` формата `3:2` с женщиной в центральной безопасной зоне: лицо и шляпа всегда должны оставаться полностью видимыми. На desktop пункты списка используют компактные внутренние отступы, а видимая высота иллюстрации подстраивается под естественную суммарную высоту списка. На tablet/mobile обе композиции перестраиваются в одну колонку; иллюстрация кадрируется в квадрат по центру без обрезания лица или шляпы.
- В блоке `Mehr Wege, auf denen der Wert zurückkommt` первый абзац растягивается на всю доступную ширину, после него располагается декоративная лента локальных артов Fortune Wheel, Prize Drops, Pragmatic Play Drops & Wins и BGaming Drops из `public/bonuses/special` на тёмной внутренней поверхности с оранжевой рамкой по паттерну Main. Второй абзац следует после ленты, не использует отдельную подложку и также занимает всю доступную ширину.
- Вложенные заголовки VIP `Wie du verdienst` и `Die Stufen und was sie wert sind` используют иконку огня `public/icons/fire.svg`. Вложенные заголовки турниров `Was läuft` и `Wie du mitmachst` используют иконку карт `public/content-icons/cards.png`.
- Существующие изображения и карточки блоков `Das Willkommenspaket, auf zwei Arten`, `Reload-Boni`, `Freispiele über den ganzen Kalender` и `Mehr Wege, auf denen der Wert zurückkommt` сохраняются без перегенерации и замены ассетов.
- Новая редакционная структура не содержит таблиц и Cashback-раздела старой Bonuses-страницы. Между контентными секциями используются четыре общих промобаннера по паттерну Main: `first-deposit` после блока `Warum sich die Aktionen hier für deutsche Spieler lohnen`, `second-deposit` после `Das Willkommenspaket, auf zwei Arten`, `friday-reload` после `Reload-Boni` и `highroller` после `Treuebelohnungen & VIP-Vorteile`. Баннер приложения с мобильным телефоном на странице Bonuses не используется.
- Заголовки секций допускают естественный перенос на всех ширинах и не создают горизонтального переполнения.
- Текстовый контейнер Hero Bonuses на desktop ограничен шириной `1000px`; на tablet/mobile возвращается к общему адаптивному правилу Hero с доступной шириной до `860px`.
- Подраздел `Die Bedingungen, die man kennen sollte` не использует отдельную внутреннюю подложку, рамку или дополнительный карточный padding и остаётся частью общей поверхности блока `Das Willkommenspaket, auf zwei Arten`.
- Финальная CTA блока `Dein Angebot wählen` использует подпись `Bonus Holen` и бонусный affiliate URL. Hero сохраняет CTA `Jetzt beitreten`.

### 4.3. App

Источник текста: `Тексты/National Casino Germany App.docx`.

Страница состоит из 9 контентных блоков, соответствующих одному заголовку H1 и восьми заголовкам H2 исходного DOCX. Поскольку единственный H1 страницы уже находится в Hero, редакционный H1 `Die National Casino App in Deutschland` отображается в контенте как H2 без изменения текста. Порядок блоков, вложенность подразделов H3, последовательность абзацев, списков и таблицы сохраняются строго по исходному файлу. Hero, общие Header, Sidebar и Footer в эти 9 блоков не входят.

#### 4.3.1. Hero App

- Заголовок из строки `META TITLE`: `National Casino App Deutschland: Web-App holen & spielen`.
- Текст описания из строки `META DESCRIPTION`: `Installiere die National Casino App direkt von der Seite — kein App Store, keine APK-Suche. Ein Konto, alle Spiele und 10 Freispiele fürs Einrichten.`
- Над заголовком отображаются хлебные крошки `Startseite → App`.
- Основная CTA-ссылка Hero сохраняет общую подпись `Jetzt beitreten`.
- Hero использует новую desktop-композицию `public/content/app/hero-v2.webp` формата `16:9`: мужчина и женщина вместе располагаются справа, слева сохраняется затемнённая сейф-зона под текст. На tablet/mobile подключается отдельная вертикальная композиция `public/content/app/hero-mobile-v2.webp` формата `2:3`, в которой оба персонажа сгруппированы плотнее. В обоих вариантах лица, головы и шляпа полностью остаются внутри кадра. Ширина текстового контейнера App Hero на desktop составляет максимум `880px`, а на узких экранах ограничивается доступной шириной.

#### 4.3.2. Структура контентных блоков

1. `Die National Casino App in Deutschland`
   - два вступительных абзаца о формате Web-App и назначении страницы.
2. `Was die App tatsächlich ist`
   - четыре абзаца о PWA, общем аккаунте, автоматических обновлениях, безопасной установке и 10 Freispielen.
3. `So installierst du sie`
   - вводный абзац;
   - подраздел `Auf Android`, вводная строка, нумерованный список из 8 действий и заключительный абзац;
   - подраздел `Auf iPhone und iPad`, вводная строка, нумерованный список из 8 действий и заключительный абзац.
4. `Ein klares Wort zu APK-Dateien`
   - два абзаца о рисках сторонних APK и установке только с официальной страницы.
5. `Was du davon hast`
   - вводный абзац;
   - подразделы `Sie ist wirklich tragbar`, `Sie ist von Grund auf sicherer` и `Sie ist für den Bildschirm gebaut, den du in der Hand hältst` с одним абзацем в каждом;
   - подраздел `Sie kommt mit ihrer eigenen Belohnung`, вводный абзац, нумерованный список из 5 действий и заключительный абзац.
6. `Was dein Gerät braucht`
   - вводный абзац;
   - маркированный список из 6 требований;
   - два заключительных абзаца об установке без Store/APK и требованиях к соединению.
7. `Die mobile Seite, falls du lieber nichts installierst`
   - два вводных абзаца;
   - две сравнительные карточки `Mobile Seite / Installierte App`, каждая с 6 пунктами;
   - заключительный абзац о едином профиле и выборе способа доступа.
8. `Häufig gestellte Fragen`
   - 7 вопросов с ответами в порядке исходного DOCX.
9. `Verantwortungsvoll spielen`
   - два абзаца о доступных инструментах, личном бюджете и источниках помощи.

#### 4.3.3. Навигация App

Sidebar и его компактная tablet/mobile-версия ведут ко всем 9 контентным блокам в том же порядке и используют их заголовки без сокращения или создания новых редакционных названий.

#### 4.3.4. Композиция App

- Страница использует те же поверхности, внутренние отступы, заголовки, таблицы, списки, кнопки и адаптивные правила, что Main и Bonuses; отдельная визуальная система для App не создаётся.
- Контентные секции App используют системный внутренний отступ `20px`. Их высота определяется фактическим содержимым: фоновые блоки не получают дополнительную жёсткую `min-height`, создающую пустое пространство между последним текстовым элементом и нижней границей секции.
- CTA `Jetzt beitreten` отображается в конце блоков `Was die App tatsächlich ist`, `Ein klares Wort zu APK-Dateien` и `Die mobile Seite, falls du lieber nichts installierst`. В блоках `Die National Casino App in Deutschland`, `So installierst du sie`, `Was du davon hast`, `Was dein Gerät braucht`, `Häufig gestellte Fragen` и `Verantwortungsvoll spielen` CTA не отображается.
- Блок `Die National Casino App in Deutschland` использует полноширинную композицию `public/content/app/intro-v2.webp` формата `16:9` с женщиной справа, затемнением и текстом слева.
- Блок `Was die App tatsächlich ist` использует двухколоночную композицию: текст слева, вертикальная иллюстрация `public/content/app/general-install-v2.webp` с мужчиной справа. На desktop высота иллюстрации определяется высотой соседней текстовой колонки; на tablet/mobile колонки перестраиваются в одну, и иллюстрация получает адаптивную минимальную высоту.
- Блок `So installierst du sie` построен из двух зеркальных строк по паттерну `Aktionscodes: Dein Ticket für etwas Extra`: Android — вертикальная иллюстрация `public/content/app/android-install-v2.webp` с женщиной слева и инструкция справа; iOS — инструкция слева и вертикальная иллюстрация `public/content/app/ios-install-v2.webp` с мужчиной справа. Под каждым нумерованным списком располагается соответствующая существующая кнопка загрузки приложения с локальной иконкой. На tablet/mobile строки перестраиваются в одну колонку с сохранением внутреннего порядка элементов.
- Блок `Ein klares Wort zu APK-Dateien` использует полноширинный фон `public/content/app/apk-warning-v2.webp` с женщиной справа, затемнение и текст слева.
- Блок `Was du davon hast` не использует фоновое изображение. Вводный и заключительный абзацы занимают всю доступную ширину; первые три преимущества оформлены отдельными карточками в двухколоночной сетке на desktop и одной колонкой на tablet/mobile, при этом третья карточка занимает всю ширину сетки. `Sie kommt mit ihrer eigenen Belohnung` является обычным заголовком без иконки и обводки с вводным текстом над списком. Нумерованный список располагается слева от панорамной иллюстрации `public/content/app/app-reward-v3.webp` формата около `2.1:1` с мужчиной в центральной композиции; на desktop видимая высота иллюстрации всегда равна естественной высоте соседнего списка, а на tablet/mobile элементы перестраиваются в одну колонку и изображение сохраняет широкую пропорцию. Голова и лицо персонажа полностью остаются внутри кадра. Заголовки трёх карточек преимуществ используют карточную локальную иконку и тонкий золотой разделитель.
- Блок `Was dein Gerät braucht` использует полноширинную композицию `public/content/app/requirements-v2.webp` с женщиной справа по паттерну `Verantwortungsvoll spielen`: текст и список располагаются слева поверх затемнения, смысловая часть изображения остаётся справа; отдельной правой колонки изображения нет.
- Сравнение в блоке `Die mobile Seite, falls du lieber nichts installierst` оформлено двумя равными карточками `Mobile Seite` и `Installierte App`: заголовки используют локальную иконку cards, каждый из шести пунктов — общую иконку огня. На tablet/mobile карточки перестраиваются в одну колонку.
- Вопросы блока `Häufig gestellte Fragen` используют существующий раскрывающийся FAQ-паттерн и те же данные в видимой части и JSON-LD.
- Блок `Verantwortungsvoll spielen` использует полноширинный фон `public/content/app/responsible-play-v2.webp` с мужчиной справа, затемнение и текст слева.
- Во всех новых композициях App персонажи чередуются, а лица, головы, волосы и шляпа находятся внутри расширенной безопасной зоны и не обрезаются ни на desktop, ни при адаптивном кадрировании.
- Четыре существующих промобаннера сохраняются после 2-го, 4-го, 6-го и 8-го редакционных блоков соответственно: `first-deposit`, `second-deposit`, `highroller`, `friday-reload`.
- Общий баннер приложения сохраняется сразу после блока `Was du davon hast`.
- Все баннеры сохраняют текущие компоненты и дизайн без изменений.

## 5. Общие элементы сайта

Для немецкой версии сайта утверждены следующие интерфейсные подписи; они имеют приоритет над английскими подписями, упомянутыми ниже в описании исходной композиции:

- header: `Main` → `Startseite`, `Bonuses` → `Boni`, `App` → `App`, `Log In` → `Anmelden`, `Register now` → `Registrieren`;
- платформенный блок sidebar: `Fortune wheel` → `Glückrad`, `Missions` → `Missionen`, `Games` → `Spiele`, `Live dealers` → `Live-Dealer`, `Promotions` → `Aktionen`, `Tournaments` → `Turniere`, `Hall of fame` → `Ruhmeshalle`, `VIP` → `VIP`, `Application` → `Anwendung`, `Download our app!` → `Lade unsere App herunter!`; заголовок `Navigation` сохраняется без изменения;
- CTA в Hero, контентных блоках и баннерах: `Join now` → `Jetzt beitreten`, `Get bonus` → `Bonus Holen`;
- рейтинг Hero Main: `Overall Rating` → `Gesamtbewertung`; хлебные крошки используют актуальные названия меню: `Startseite → Boni`, `Startseite → App` и `Startseite → VERANTWORTUNGSVOLLES SPIELEN` для соответствующих страниц;
- кнопки загрузки приложения: `Download the iOS App` → `Lade die iOS-App herunter`, `Download the Android App` → `Lade die Android-App herunter`;
- промобаннеры: `Einzahlungsbonus` — `100 % bis zu 100 EUR + 100 Freispiele`; `Zweite Einzahlungsbonus` — `50 % bis zu 200 EUR + 50 Freispiele`; `Highroller-Bonus` — `100 % bis zu 750 EUR`; `Freitags-Reload-Bonus` — `50 % bis zu 250 EUR + 100 Freispiele`;
- footer: `VERANTWORTUNGSVOLLES SPIELEN`, `ALLGEMEINE GESCHÄFTSBEDINGUNGEN`, `DATENSCHUTRICHTLINIE`, `BONUSBEDINGUNGEN`, `FAQ`, `ÜBER UNS`, `KONTAKT`;
- индикатор локации в desktop/mobile header использует локальный флаг Германии `public/brand/flag-de.svg` и текст `DE`;
- footer отображает строку copyright `© {текущий год} National Casino Deutschland.`; год вычисляется автоматически на сервере;
- юридический текст footer: `Diese Website wird als Zahlungsabwickler verwaltet und betrieben von TechSolutions (CY) Group Limited (Registrierungsnummer HE 377018) mit eingetragenem Sitz in Parthenonos 5, Apartment 103, 2020, Nikosia, Zypern und von der TechSolutions Group N.V. (Registrierungsnummer 144920 (0)) als Lizenzinhaber (8048/JAZ2017-067) mit der Adresse in Dr. H. Fergusonweg 1, Curacao betrieben.`;
- предупреждение footer: `Das Glücksspiel kann süchtig machen. Spielen Sie verantwortungsvoll.`.

### 5.1. Header

Header является общим для всех страниц и всегда закреплён у верхнего края окна при прокрутке сайта.

Desktop-макет `docs/source/1728w default.pdf` задаёт высоту header `73px` при ширине макета `1728px`. Это значение используется как основное desktop-значение вместо ранее ориентировочного размера `85px`.

Предусмотреть:

- `position: sticky` или эквивалентное поведение с фиксацией у верхнего края;
- достаточный `z-index`, чтобы header располагался поверх контента;
- непрозрачный или полупрозрачный тёмный фон, сохраняющий читаемость элементов при прокрутке;
- логотип в левой части header;
- навигацию по страницам Main, Bonuses и App рядом с логотипом;
- начало desktop-навигации выравнивается по левой границе внутренней области основного контента с учётом ширины sidebar, межколоночного расстояния и внутреннего отступа контента;
- расстояние между логотипом и desktop-навигацией формируется этим выравниванием и должно быть визуально больше обычного межэлементного отступа;
- пункты desktop-навигации разделяются фирменными декоративными маркерами: золотым ромбом с короткими тонкими горизонтальными линиями с обеих сторон; маркеры являются декоративными, не кликабельны и скрываются вместе с desktop-навигацией на tablet/mobile;
- визуально оформленную как вторичная кнопка внешнюю ссылку `Log In` в правой части header;
- визуально оформленную как основная кнопка внешнюю ссылку `Register now` в правой части header;
- индикатор текущей локации у самого правого края header;
- флаг Германии внутри индикатора локации;
- флаг отображается в круглом контейнере размером около `32×32px`; использовать локальный SVG-файл `public/brand/flag-de.svg`, изображение заполняет окружность через `object-fit: cover`;
- текстовое сокращение `DE` рядом с флагом;
- адаптивное меню для мобильных устройств;
- визуально различимое активное состояние текущего пункта меню.

Порядок элементов в desktop-версии слева направо:

1. Логотип.
2. Main.
3. Bonuses.
4. App.
5. Свободное пространство.
6. Log In.
7. Register now.
8. Флаг Новой Зеландии и сокращение `NZ`.

Ссылка `Register now` является основным CTA и использует фирменное золотое оформление. Ссылка `Log In` располагается перед ней, является вторичной и не должна конкурировать с основным CTA. Обе ссылки ведут на общий внешний affiliate URL, зафиксированный в разделе 6.2.6.

Для кнопок авторизации используются подписи `Log In` и `Register now`.

Индикатор с изображением флага Новой Зеландии и текстом `NZ` показывает текущую локацию сайта. Он не является переключателем языка, не содержит стрелку и не открывает список языков или стран.

При переходе к мобильной версии навигация и кнопки авторизации могут переноситься в раскрываемое меню. Логотип и элемент открытия мобильного меню должны оставаться доступными в закреплённом header.

<!-- TODO: добавить точные горизонтальные отступы и breakpoint мобильного меню -->

### 5.2. Левый sidebar

Левый sidebar является общим desktop-навигационным паттерном для страниц Main, Bonuses и App, но его пункты формируются отдельно на основании блоков конкретной страницы.

Desktop-макет `docs/source/1728w default.pdf` задаёт ширину sidebar `243px` при ширине макета `1728px`. Это значение используется как основное desktop-значение вместо ранее ориентировочного размера `250px`.

Предусмотреть:

- фиксированное или `sticky`-положение под закреплённым header;
- заголовок списка `Navigation`;
- кнопку-индикатор со стрелкой в строке заголовка, позволяющую раскрывать и закрывать список;
- вертикальный список якорных ссылок;
- пункты списка отображаются только текстом, без порядковых номеров;
- расстояние между соседними пунктами списка — `8px`;
- активное состояние текущего блока;
- состояния `hover` и `focus-visible` для всех ссылок;
- плавный переход к выбранному блоку с учётом настройки `prefers-reduced-motion`;
- достаточный контраст активного и неактивных пунктов;
- ограничение высоты и внутреннюю прокрутку sidebar, если список пунктов не помещается в окно;
- отсутствие перекрытия footer и другого контента в конце страницы.

Активный пункт оформляется отдельной контрастной строкой на приподнятой тёмной поверхности с фирменной золотой полосой у левого края. Карточка активного пункта сохраняет читаемый светлый текст и не использует яркую заливку всей строки золотым цветом.

Sidebar не должен закрывать основной контент или уменьшать его до неудобной ширины.

На tablet и mobile постоянный левый sidebar необходимо заменить компактным меню разделов страницы. Компактная версия должна сохранять доступ ко всем якорным ссылкам и показывать текущий активный раздел.

#### 5.2.1. Состав sidebar из PDF-референса

В desktop-макете зафиксированы следующие пункты и элементы sidebar:

1. `Fortune wheel` — верхний акцентный пункт.
2. `Missions` — верхний акцентный пункт.
3. `Games`.
4. `Live dealers`.
5. `Promotions` с числовым badge.
6. `Tournaments` с числовым badge.
7. `Hall of fame`.
8. `VIP`.

В нижней части sidebar в PDF также показаны отдельные информационные карточки:

- `Application` с пояснением `Download our app!`;
- `Latest winners`.

Этот список фиксирует структуру оригинального sidebar как дизайн-референс. Редакционная якорная навигация по-прежнему формируется только из реально существующих блоков соответствующей страницы. Отдельно утверждённый ниже платформенный блок является интерфейсным исключением: его названия и badges переносятся из PDF по прямому требованию и не считаются источником редакционного контента страницы.

Над раскрываемым списком `Navigation` в desktop-sidebar необходимо воспроизвести фирменный платформенный блок из `docs/source/Главная страница.pdf` в следующем порядке:

1. Акцентные промо-плашки-ссылки `Fortune wheel` и `Missions` с фирменной золотисто-персиковой заливкой.
2. Список внешних ссылок `Games`, `Live dealers`, `Promotions`, `Tournaments`, `Hall of fame` и `VIP`. Пункты `Sports betting` и `Live betting` не отображаются.
3. `Games` и `Live dealers` являются обычными пунктами меню: они не раскрываются и не содержат стрелок или других индикаторов раскрытия.
4. У `Promotions` показывается badge `14`, у `Tournaments` — badge `3`.
5. Пункт `Support` в платформенном блоке sidebar не отображается.
6. Ниже списка располагается внешняя ссылка-карточка `Application` с текстом `Download our app!` и оригинальным изображением приложения.

Для промо-плашек `Fortune wheel` и `Missions`, иконок пунктов платформенного меню, desktop/mobile-логотипов и флага Новой Зеландии используются локальные копии оригинальных ассетов с `https://natiocazino.com/`. Иконка `Fortune wheel` собирается из неподвижной рамки `70×70px` и отдельного колеса `65×65px`; колесо плавно вращается с линейным циклом `7s`, а при `prefers-reduced-motion: reduce` анимация отключается. Размеры и взаимное позиционирование двух частей соответствуют оригинальному sidebar. Иконки `Games`, `Live dealers`, `Promotions`, `Tournaments`, `Hall of fame` и `VIP` берутся из фирменного иконочного шрифта `national-casino`. Самодельные CSS-имитации этих изображений и иконок не использовать.

Этот платформенный блок является отдельным интерфейсным элементом и не заменяет редакционную якорную навигацию `Navigation`. `Promotions` ведёт на бонусный affiliate URL, а остальные пункты, промо-плашки и `Application` — на общий affiliate URL. Для карточки `Application` действует исключение из общего правила серых заглушек: несмотря на то, что в актуальной версии оригинального сайта эта карточка заменена блоком `Latest winners`, в sidebar используется её оригинальное изображение приложения, извлечённое из сохранённого PDF-макета оригинальной страницы. Локальный файл — `public/sidebar/application.png`; изображение заполняет правую графическую область карточки через `object-fit: cover`, не имеет серого фона заглушки и не меняет утверждённые размеры блока. На tablet/mobile платформенный блок скрывается вместе с постоянным sidebar; пользователю остаётся доступно компактное меню якорных разделов.

Пункты платформенного списка `Games`–`VIP` при наведении используют тот же эффект, что ссылки раскрываемого блока `Navigation`: скруглённую полупрозрачную приподнятую поверхность `rgb(78 72 66 / 55%)` и плавный переход цвета и фона за `160ms`.

<!-- TODO: добавить оформление активного пункта и breakpoint адаптивной версии -->

### 5.3. Footer

Footer является общим для всех страниц. На текущем этапе он воспроизводит footer актуальной английской версии оригинального сайта `https://natiocazino.com/` и служит базой для последующего ручного редактирования.

Структура сверху вниз:

1. Фирменный горизонтальный разделитель с золотыми линиями и центральным геометрическим ромбом.
2. Центрированная навигация в несколько строк с золотыми вертикальными разделителями.
3. Центрированный ряд платёжных систем Visa и Mastercard на светлых карточках.
4. Тонкий золотистый горизонтальный разделитель.
5. Круглый красный индикатор `18+`.
6. Полноширинный центрированный текстовый блок из двух абзацев:
   - `This website is managed and operated by TechSolutions (CY) Group Limited (reg.number HE 377018) with registered office located at Parthenonos 5, Flat 103, 2020, Nicosia, Cyprus as a Billing Agent and operated by TechSolutions Group N.V. (registration number 144920 (0)) as a licence holder (8048/JAZ2017-067) with address at Dr. H. Fergusonweg 1, Curacao.`;
   - `Gambling can be addictive. Play responsibly.`.
7. Центрированный графический знак сертификации `GCB` с подписью `cert.gcb.cw`, расположенный ниже последнего текстового абзаца. Использовать локальный файл `public/footer/gcb-validator.png` без изменения пропорций. Ширина знака — около `86px`, то есть на четверть меньше ширины карточек платёжных систем (`114px`).

Региональная перелинковка по странам на текущем этапе не отображается.

Текстовый блок располагается после индикатора `18+`, занимает всю доступную ширину footer и имеет центральное выравнивание. Между юридическим текстом и предупреждением об ответственной игре сохраняется отдельный вертикальный интервал.

Пункты footer и их ссылки:

- `VERANTWORTUNGSVOLLES SPIELEN` — `/responsible-gaming`;
- `ALLGEMEINE GESCHÄFTSBEDINGUNGEN` — `/terms-and-conditions`;
- `DATENSCHUTRICHTLINIE` — `/privacy-policy`;
- `BONUSBEDINGUNGEN` — `/bonus-terms`;
- `FAQ` — `/faq`;
- `ÜBER UNS` — `/about-us`;
- `KONTAKT` — `/contact`.

Порядок пунктов фиксирован и соответствует утверждённому референсу. Дополнительные пункты footer не отображаются.

Использовать локальные копии оригинальных логотипов Visa и Mastercard, а также утверждённого знака GCB из `public/footer`. Пункты Main, Bonuses и App временно не добавляются в скопированный footer; их финальное размещение будет определено при последующем ручном редактировании.

### 5.4. Hero

Hero является обязательным первым контентным экраном на всех трёх страницах сайта: Main, Bonuses и App. Он располагается после общего header и перед первым основным контентным блоком страницы.

Источник данных Hero для каждой страницы — соответствующий текстовый DOCX-файл:

| Страница | Источник |
| --- | --- |
| Main | `Тексты/National Casino Germany Main Page.docx` |
| Bonuses | `Тексты/National Casino Germany Bonuses.docx` |
| App | `Тексты/National Casino Germany App.docx` |

Правила переноса данных:

- строка, подписанная в DOCX как `Meta title`, используется как заголовок Hero;
- строка, подписанная в DOCX как `Meta description`, используется как текст описания Hero;
- служебные подписи `Meta title:` и `Meta description:` на сайте не отображаются;
- текст переносится без перевода, дополнения, сокращения или изменения формулировок;
- левый край текста Hero совпадает с левым краем текста основных контентных секций;
- заголовок H1 во всех Hero использует тот же вертикальный золотисто-оранжевый градиент `#FFC700 → #FF8A00`, что и основное предложение общих промо-баннеров; текст описания под заголовком отображается белым цветом `#FFFFFF`;
- Hero Main всегда содержит строку `Overall Rating 4.4/5` над заголовком и не содержит хлебных крошек;
- Hero всех остальных страниц содержит над заголовком хлебные крошки `Home → название текущей страницы`: `Home` ведёт на `/`, текущая страница не является ссылкой и отмечается `aria-current="page"`;
- на desktop заголовок и описание Hero оформляются каждый в две строки; на узких экранах допускается дополнительный естественный перенос без горизонтального переполнения;
- текстовый контейнер Hero на desktop ограничен максимальной шириной `860px`, кроме утверждённого исключения Hero Bonuses шириной `1000px`; на tablet/mobile все Hero занимают доступную ширину с ограничением до `860px` и сохранением адаптивных внутренних отступов;
- Hero Main использует локальный фон `public/content/main/hero-characters-v2.webp` в пропорции `16:9`: женщина и мужчина из утверждённых референсов вместе держат золотые монеты в мистическом зале казино, вся смысловая композиция находится справа, а слева сохраняются тёмная сейф-зона и существующий градиентный overlay под текст;
- Hero не изменяет установленный порядок основных контентных блоков страницы и не считается одним из них;
- конкретные значения Hero для Bonuses и App фиксируются при утверждении структуры соответствующих страниц.

### 5.5. Кнопка возврата наверх

На всех маршрутах сайта отображается общая фиксированная круглая кнопка возврата к началу страницы. Кнопка располагается в правом нижнем углу viewport, появляется после прокрутки более чем на `360px` и остаётся поверх контента без изменения его компоновки.

Кнопка использует фирменный золотисто-персиковый градиент, тёмную стрелку вверх, тонкую внутреннюю окантовку и мягкую тень. Основной desktop-размер — `52×52px`, mobile-размер — `46×46px`. На desktop отступы справа и снизу составляют не менее `20px` и `24px`, на mobile — `16px`.

При активации кнопка плавно прокручивает страницу к самому верху. При `prefers-reduced-motion: reduce` переход выполняется без анимации. Кнопка имеет accessibility-подпись `Back to top`, доступна с клавиатуры только в видимом состоянии и использует общий заметный `focus-visible` сайта.

## 6. Дизайн и визуальный стиль

### 6.1. Общее направление

- современный стиль;
- премиальная подача;
- ощущение доверия и надёжности;
- без визуального перегруза типичными казино-эффектами;
- основной акцент сайта — казино;
- беттинг затрагивается в пределах предоставленного текста.

### 6.2. Брендовые материалы

#### 6.2.1. Логотип

Использовать фирменный логотип National Casino с оригинального сайта бренда.

Структура логотипа:

- знак в форме щита или шестигранного жетона;
- буква `N` внутри знака;
- металлическая золотая окантовка и золотые детали;
- белый текстовый знак `National casino`, расположенный в две строки.

Правила использования:

- на desktop использовать официальный горизонтальный файл `public/brand/national-casino-logo.svg` с оригинального сайта; отображаемый размер — около `128×50px`;
- на tablet и mobile при ширине viewport до `1180px` использовать официальный компактный файл `public/brand/national-casino-mark.svg` с оригинального сайта; отображаемый размер — около `46×50px`;
- размещать логотип только на фоне с достаточным контрастом;
- не изменять пропорции, начертание и взаимное расположение элементов;
- не перекрашивать логотип произвольными цветами;
- предусмотреть безопасное пространство вокруг логотипа;
- не собирать знак и название бренда средствами CSS или текстовыми элементами, если доступен соответствующий готовый файл логотипа.

Оба файла имеют прозрачный фон и используются без изменения исходного соотношения сторон.

#### 6.2.2. Цветовая палитра

Базовая палитра строится на сочетании тёплых тёмно-коричневых поверхностей, светлой типографики, золотого акцента и персикового градиента.

Основным источником точных цветов является desktop-макет `docs/source/1728w default.pdf`. Значения ниже извлечены из векторных объектов PDF, имеют приоритет над визуально подобранными значениями из скриншотов и должны использоваться в реализации как явные дизайн-токены.

| Роль | Значение | Использование |
| --- | --- | --- |
| Фон header и footer | `#292526` | закреплённый header, его навигационная поверхность и общий footer |
| Фон страницы и базовой оболочки | `#000000` | body, центрированная оболочка и область page-layout |
| Фон sidebar и вторичной навигации | `#2A2829` | sidebar и вторичные навигационные области |
| Основная поверхность | `#322E2F` | панели, карточки, поля и контейнеры контента |
| Фон внешних контентных секций | `#000000` | все основные блоки контента Main, Bonuses и App |
| Контрастная обводка секций и медиаподложек | `#FF9100` | внешний периметр контентных секций, таблиц и вложенных подложек изображений/игровых рядов |
| Поднятая тёплая поверхность | `#4E4842` | активные панели, строки и визуально поднятые элементы |
| Самый тёмный тёплый нейтральный | `#231F20` | глубокие подложки и тёмный текст на светлых акцентах |
| Почти чёрный | `#111111` | мелкие тёмные детали и высококонтрастный текст при необходимости |
| Основной светлый текст | `#FFFAF3` | заголовки и основной контрастный текст |
| Чистый белый | `#FFFFFF` | отдельные контрастные элементы и иконки |
| Холодный светлый нейтральный | `#F6F7F9` | светлые служебные элементы интерфейса |
| Вторичный светлый текст | `#D4C5B1` | основной вторичный текст и иконки на тёмном фоне |
| Приглушённая навигация | `#94897A` | неактивные пункты меню, подписи и вспомогательные иконки |
| Нейтральный disabled | `#757575` | недоступные и малоприоритетные элементы |
| Основной золотой акцент | `#FEC882` | заголовки, обводки, иконки, активные пункты и декоративные линии |
| Персиковый акцент | `#FF9F6C` | нижняя точка CTA-градиента и выделенные промо-элементы |
| Сигнальный красный | `#FE5151` | предупреждения, responsible gaming и маркировка `18+` |

Основной градиент CTA:

```css
linear-gradient(180deg, #FEC882 0%, #FF9F6C 100%)
```

Градиент используется для основного CTA и отдельных выделенных промо-элементов. Однотонный цвет `#FEC882` используется для обводок, заголовков, иконок, активных пунктов и линейного декора. Акцентные цвета используются дозированно и не применяются как массовый фон для крупных текстовых областей.

Цвета платёжных систем и стороннего контента, встречающиеся в PDF, не входят в фирменную палитру National Casino. В частности, `#FF5F00`, `#EB001B` и `#F79E1B` относятся к платёжной айдентике и не должны использоваться как общие дизайн-токены сайта.

#### 6.2.3. Шрифты

Основной фирменный шрифт: `Poppins`, запасной шрифт — `sans-serif`.

Используемые насыщенности:

- `400` — основной текст;
- `500` — выделенный вспомогательный текст и элементы навигации;
- `600` — кнопки, метки и интерфейсные акценты;
- `700` — основные заголовки.

Шрифт должен подключаться через оптимизированный механизм Next.js. В реализации использовать только необходимые начертания.

#### 6.2.4. Типографические приёмы

Desktop-макет подтверждает использование `Poppins` и следующую шкалу размеров. Размеры являются исходными desktop-ориентирами; на tablet/mobile допускается адаптивное уменьшение крупных заголовков при сохранении иерархии.

| Размер | Роль в PDF-референсе |
| --- | --- |
| `50px` | крупный промо-заголовок live casino |
| `35px` | hero-заголовок и крупная сумма промо |
| `30px` | основные заголовки секций |
| `24px` | заголовки карточек, отзывов и материалов |
| `22.5px` | дополнительная крупная сумма или промо-акцент |
| `20px` | заголовки подразделов и преимуществ |
| `18px` | выделенные названия игр, карточек и служебных блоков |
| `16px` | навигация header, кнопки и основной интерфейсный текст |
| `15px` | навигация footer |
| `14px` | основные пункты sidebar и компактный текст |
| `13px` | предупреждения responsible gaming в footer |
| `12px` | малые подписи, метки и компактные карточки |
| `10px` | микротекст, RTP и пояснения sidebar |
| `9px` | минимальные служебные метки |

В реализации следует использовать только те уровни шкалы, которые требуются фактической структурой и контентом страницы, без искусственного добавления новых текстовых ролей.

- Главные промо-заголовки могут использовать прописные буквы.
- Крупные заголовки — насыщенность `700`, плотный межстрочный интервал и слегка уменьшенный `letter-spacing`.
- Ориентир для плотного заголовка: `letter-spacing: -0.5px`.
- Основной текст — насыщенность `400`, с увеличенным межстрочным интервалом для удобства чтения длинных материалов.
- Вторичный текст выполняется приглушёнными серо-фиолетовыми оттенками.
- Кнопки и короткие интерфейсные метки могут использовать прописные буквы, насыщенность `600` и слегка увеличенный `letter-spacing`.
- В пределах одной секции должна сохраняться ясная иерархия H2, H3, основного текста, подписей и меток.
- Длинные материалы не должны превращаться в сплошные текстовые стены: текст распределяется между абзацами, списками, карточками, таблицами и callout-блоками без изменения исходного содержания.
- В секции `National Casino Mobile App` на Main текстовые абзацы занимают всю доступную ширину контентного блока без ограничения `85ch`.

#### 6.2.5. Декоративные элементы

- тёмные фоновые изображения с затемняющим слоем;
- деликатные тематические паттерны с низким контрастом;
- тонкие горизонтальные разделители на тёмных поверхностях;
- металлические золотые градиенты;
- мягкие внутренние блики на золотых элементах;
- сдержанные тени под интерактивными элементами;
- тонкие золотые или полупрозрачные обводки;
- крупные скруглённые поверхности и карточки;
- мягкое цветовое свечение у отдельных акцентных карточек.

Контентные секции, карточки, панели, таблицы, промо-баннеры и навигационные поверхности не используют внешние `box-shadow`. Допускаются только внутренние блики и сдержанные тени интерактивных кнопок, небольших иконок и декоративных элементов.

Декор не должен мешать чтению длинного контента. Анимации свечения и градиентов допускаются только как ненавязчивый акцент и должны учитывать `prefers-reduced-motion`.

#### 6.2.6. Кнопки

Основная кнопка:

- фон — основной персиково-золотой градиент;
- цвет текста — `#231F20`;
- насыщенность — `600`;
- текст может быть выполнен прописными буквами;
- слегка увеличенный `letter-spacing`;
- скругление — ориентировочно `7–8px`;
- внутренний светлый блик;
- мягкая внешняя тень;
- высокий контраст с тёмным фоном.

Ориентир эффекта основной кнопки:

```css
box-shadow:
  inset 0 2px 3px #FBE2C2,
  0 4px 4px rgb(0 0 0 / 50%);
```

Вторичная кнопка:

- тёмный или прозрачный фон;
- светлый текст;
- тонкая обводка `#FEC882`;
- те же высота, радиус и типографический ритм, что у основной кнопки;
- визуальный приоритет ниже, чем у основного CTA.

Для всех кнопок и визуально оформленных как кнопки CTA-ссылок определить состояния `default`, `hover`, `focus-visible`, `active` и при необходимости `disabled`. Интерактивные состояния должны быть заметными, но не использовать чрезмерные вспышки или сильное свечение.

В контентных карточках и промо-блоках кликабельной является только явно оформленная CTA-ссылка. Сама поверхность контентной карточки не должна быть ссылкой или другим интерактивным элементом. Карточка `Application` и пункты платформенного sidebar являются отдельно утверждёнными исключениями.

На странице Bonuses каждая из пяти бонусных карточек в блоках `Das Willkommenspaket, auf zwei Arten` и `Reload-Boni`, блок `Warum sich die Aktionen hier für deutsche Spieler lohnen` и итоговый блок `Dein Angebot wählen` получают CTA `Bonus Holen` с бонусным affiliate URL. Карточки блока `Freispiele über den ganzen Kalender` CTA не получают. Блоки `Das Willkommenspaket, auf zwei Arten`, `Treuebelohnungen & VIP-Vorteile` и `Turniere und Bestenlisten` получают CTA `Jetzt beitreten` с общим affiliate URL. Блоки `National Casino Boni & Aktionen in Deutschland`, `Reload-Boni`, `Freispiele über den ganzen Kalender`, `Mehr Wege, auf denen der Wert zurückkommt`, `Aktionscodes: Dein Ticket für etwas Extra`, `Zeitlich begrenzte und saisonale Angebote` и `Verantwortungsvoll spielen` не получают секционных CTA. Остальные страницы сохраняют собственные правила CTA, закреплённые в их разделах.

Все CTA, ведущие на внешний affiliate-сайт, реализуются тегом `<a>` с заполненным `href`, а не тегом `<button>`, при полном сохранении утверждённого визуального оформления кнопок. Используются два URL:

- бонусный URL `https://media.toxtren.com/redirect.aspx?pid=159513&bid=2036&lpid=2084` — для `Get bonus`, CTA бонусных баннеров, бонусных карточек и пункта `Promotions` в платформенном sidebar;
- общий URL `https://media.toxtren.com/redirect.aspx?pid=159513&bid=2036&lpid=2081` — для `Join now`, `Log In`, `Register now`, кнопок загрузки Android/iOS, `Fortune wheel`, `Missions`, `Application` и остальных пунктов платформенного sidebar. Пункты footer используют только внутренние маршруты, закреплённые в разделе 5.3.

Во всех пользовательских кликабельных переходах допускаются только эти два внешних affiliate URL. Остальные внешние адреса не используются в `href`. Это ограничение не относится к техническим URL в metadata, canonical, JSON-LD Schema и проектной документации, а также к внутренним маршрутам и якорным ссылкам сайта.

Настоящими `<button>` остаются только действия внутри интерфейса, не являющиеся внешней навигацией: открытие мобильного меню, управление слайдерами и возврат наверх. Внешние affiliate-ссылки получают `rel="sponsored"`.

#### 6.2.7. Сетка и поверхности

- Максимальная ширина общей оболочки сайта — `1600px`.
- На экранах шире `1600px` оболочка сайта центрируется, а свободное пространство остаётся за её пределами.
- Фон `body` за пределами центрированной оболочки совпадает с фоном оболочки `#000000`, без видимых боковых полос другого оттенка. Область `page-layout` использует тот же цвет.
- Desktop-макет выполнен для контрольной ширины viewport `1728px`; это референсная ширина, а не ограничение максимальной ширины сайта.
- Высота закреплённого header на desktop — `73px`.
- Ширина desktop-sidebar — `243px`.
- Расстояние между header и расположенной ниже областью страницы — `12px`.
- Расстояние между sidebar и основным контентом — `12px`.
- Контейнер `page-main` не имеет горизонтальных внутренних отступов (`0px`) на desktop, tablet и mobile, поэтому Hero и контентные секции доходят до краёв основной колонки. Собственные внутренние отступы контентных секций сохраняются на уровне `20px`.
- Все пространственные значения Main строятся по сетке, кратной `4px`. Базовая иерархия глубины: `20px` — секции и соседние смысловые блоки; `16px` — граница карточки и крупные группы внутри неё; `12px` — вложенные группы и интервалы между элементами карточки; `8px` — компактные контролы и внутренние пары; `4px` — тесно связанные подписи и метаданные.
- Чем глубже элемент вложен в секцию, карточку или внутреннюю группу, тем меньше его отступ от соседних связанных элементов. Карточка отзыва с группой имени и страны, оценкой, разделителем и текстом отзыва является эталоном такой иерархии. При необходимости допускаются ещё меньшие значения, если они остаются кратны `4px` и сохраняют визуальную связь.
- Основной контент занимает всю оставшуюся ширину внутри оболочки после вычета ширины sidebar и расстояния между колонками.
- Строить страницы на адаптивной колонной сетке.
- Для длинного текста ограничивать ширину строки внутри контентных блоков.
- Все внешние контентные секции используют чёрный фон `#000000` и обводку `#FF9100`. Вложенные медиаподложки того же паттерна — таблицы, снимки Lobby и Live-RTP, ряды live-игр, регистрационный снимок, панель провайдеров и медиачасть карточки оценки — используют такую же обводку `#FF9100`; внутренние карточки, CTA, header и sidebar сохраняют собственные утверждённые поверхности.
- Desktop-sidebar располагается под header с учётом отступа `12px` и не перекрывает header.
- Точные tablet/mobile breakpoints и размеры скруглений определить после подготовки макета первой страницы.

#### 6.2.8. Изображения

- Изображения должны поддерживать премиальный тёмный стиль сайта.
- Во всех блоках, где текст размещён поверх изображения, используется чёрный gradient overlay: плотное затемнение сохраняется от левого края до `35%`, затем плавно исчезает к `70%`, где заканчивается текстовая зона. Серый цвет в overlay не используется.
- Крупные контентные изображения и фоновые слои должны без пустых полос заполнять всю ширину и высоту отведённого им блока; для этого используется пропорциональное масштабирование в режиме `cover` и при необходимости настраивается позиция изображения.
- Если на изображении присутствует персонаж, его голова не должна обрезаться границей блока. Правило одинаково применяется к людям, животным и любым вымышленным героям; изображение необходимо пропорционально уменьшить или сместить так, чтобы голова полностью оставалась видимой.
- На tablet/mobile фоновое изображение адаптивно смещается к композиционной зоне с персонажем; приоритетом остаётся полная видимость головы, даже если для этого требуется дополнительная обрезка фона с противоположной стороны.
- Отдельный слой персонажа не растягивается с искажением пропорций: при конфликте между полным заполнением блока и сохранением персонажа фон заполняет блок в режиме `cover`, а персонаж масштабируется пропорционально в пределах доступной области.
- Не использовать изображения, которые визуально имитируют элементы управления.
- Все контентные изображения должны иметь осмысленный `alt`, если они не являются исключительно декоративными.
- Декоративные изображения не должны ухудшать показатели загрузки первого экрана.
- Финальный этап генерации и установки изображений начат по прямому указанию пользователя 12 августа 2026 года.
- Сгенерированные контентные изображения хранятся в `public/content/main`, `public/content/bonuses`, `public/content/app`, `public/content/responsible-gaming` и `public/content/shared`.
- Стиль изображений: премиальная тёмная semi-realistic 3D/CGI-графика по мотивам визуального языка оригинальной страницы Promotions, без встроенного рекламного текста и без имитации интерактивных элементов.
- Сюжет каждого изображения должен соответствовать тексту соответствующего блока. В полноразмерных фоновых композициях с текстом слева персонажи и основные предметы располагаются справа; под текстом сохраняется контролируемое затемнение.
- Сюжетные контентные изображения не повторяются между разными блоками и страницами; для каждого смыслового блока используется отдельная композиция. Повторное использование допускается только для утверждённых брендовых, интерфейсных и декоративных ассетов.
- В изображениях формата `1:1` и `3:4` основной персонаж располагается по центру кадра с безопасными полями вокруг головы и рук для адаптивного кадрирования.
- Изображения sidebar не генерируются и продолжают использовать только утверждённые оригинальные локальные ассеты.
- На странице Bonuses разрешено повторно использовать утверждённые локальные фоны и персонажей Main из `public/content/main/bonuses`, а также арты событий из `public/bonuses/special`, когда они прямо соответствуют упомянутому в тексте бонусу, провайдерской акции или Glücksrad.
- Утверждённые декоративные контентные иконки `public/content-icons/cards.png` и `public/content-icons/coin-1.png` используются соответственно в заголовках карточек настольных игр и карточках максимальных лимитов вывода; для них сохраняются исходные пропорции и пустой `alt`.

#### 6.2.9. Правила адаптации

- Сохранять визуальную иерархию и контраст на desktop, tablet и mobile.
- Не уменьшать основной текст до трудночитаемого размера ради сохранения desktop-компоновки.
- Карточки перестраивать из нескольких колонок в одну колонку или компактную ленту.
- Таблицы адаптировать отдельно в зависимости от типа данных.
- Длинные CTA и пункты навигации не должны обрезаться.

### 6.3. Референсы

Основной desktop-референс компоновки, размеров, типографики и палитры — файл `docs/source/1728w default.pdf`.

В качестве дополнительных ориентиров используются материалы из папки `Референсы` и дизайн-файл `docs/source/natiocazino_com_1728w_default.h2d`.

Приоритет применения:

1. Папка `Референсы` имеет приоритет при выборе комбинации, композиции и взаимного расположения контентных блоков, списков, таблиц и карточек.
2. Все остальные параметры берутся из настоящего ТЗ: палитра, типографика, размеры, сетка, отступы, логотип, кнопки, навигация, интерактивность, адаптивное поведение и правила работы с контентом.
3. `docs/source/1728w default.pdf`, `docs/source/natiocazino_com_1728w_default.h2d`, скриншоты и оригинальный сайт не используются как самостоятельные источники новых решений, если эти решения не зафиксированы в ТЗ.

Цвета, шрифты и размеры из референсов не копируются. Выбранные по референсам композиции необходимо адаптировать под дизайн-систему и точные значения National Casino, описанные в этом ТЗ. При любом противоречии вне области композиции блоков, списков, таблиц и карточек приоритет имеет ТЗ.

#### 6.3.1. Карточки

Основной референс: `Референсы/карточки.jpg`.

Использовать следующие приёмы:

- тёмная поверхность карточки;
- крупное скругление;
- небольшая иконка в отдельном верхнем блоке;
- заголовок, короткое описание и действие;
- мягкое цветовое свечение по нижней и боковым границам;
- сетка из нескольких карточек на desktop;
- единая высота карточек в пределах одной группы.

В проекте цветное свечение адаптировать преимущественно к золотым, янтарным и нейтральным оттенкам. Дополнительные цвета допускаются только для смыслового различения категорий.

Бонусные карточки страниц Main и Bonuses используют общий композиционный паттерн:

- редакционный текст располагается в контрастной панели слева;
- золотая обводка используется по внешнему периметру карточки, а текстовая панель отделяется только прозрачной тёмной подложкой и нижней границей заголовка;
- локальный фон заполняет карточку в режиме `cover`, персонаж отображается отдельным пропорциональным слоем справа;
- персонаж не перекрывает основной текст, его голова остаётся видимой, нижняя граница совпадает с нижней границей карточки;
- на desktop карточки группы располагаются в одну или две колонки согласно разделу конкретной страницы, на tablet/mobile переходят в одну колонку;
- карточки Bonuses не являются ссылками; внутри каждой бонусной карточки кликабельной остаётся только явно оформленная CTA `Bonus Holen`;

Заголовок секции `License & Local iGaming Regulations` отображается в одну строку на desktop и tablet. На узком mobile допускается перенос, если однострочное отображение создаёт горизонтальное переполнение или требует чрезмерного уменьшения шрифта.

#### 6.3.2. Пошаговые списки

Основной референс: `Референсы/списки.png`.

Использовать следующие приёмы:

- номер шага в отдельном компактном скруглённом блоке;
- текст шага в широком соседнем блоке;
- чёткое визуальное разделение шагов;
- расстояние между соседними строками пошагового списка — `4px`;
- достаточные внутренние отступы;
- выравнивание номера и текста по вертикали;
- последовательное расположение сверху вниз.

Красную палитру референса заменить на фирменные тёмные поверхности, золотые акценты и светлый текст. На мобильных экранах номер и текст должны сохранять очевидную связь и не создавать горизонтальный скролл.

#### 6.3.3. Сравнительные и бонусные таблицы

Основной референс: `Референсы/таблица.jpeg`.

Использовать следующие приёмы:

- каждая таблица занимает `100%` доступной ширины основного контентного контейнера;
- на desktop таблицу не размещать в половине колонки, узкой карточке или рядом с изображением;
- общий внешний скруглённый контейнер;
- визуально выделенная строка заголовков;
- интервал между строкой заголовков и первой строкой данных одинаков во всех таблицах и равен межстрочному интервалу `4px`; интервал создаётся как реальное расстояние между поверхностями строк, а не прозрачной границей внутри ячейки;
- визуальный внешний отступ от границы контейнера таблицы до строк одинаков сверху, снизу, слева и справа и составляет `12px`;
- каждая строка данных оформлена как отдельная поверхность;
- увеличенные вертикальные отступы;
- тонкая обводка или мягкое свечение строки;
- ясное выравнивание числовых и текстовых значений.

Красную палитру заменить на тёмно-золотую фирменную систему. Для мобильной версии допускается горизонтальный скролл внутри таблицы или преобразование каждой строки в компактную карточку, если при этом сохраняются заголовки и связь данных.

#### 6.3.4. VIP-таблицы

Основной референс: `Референсы/таблица 2.png`.

Использовать следующие приёмы:

- крупный номер VIP-уровня;
- тематическая иконка рядом с уровнем;
- выделенные значения обменного курса и награды;
- награда в отдельной компактной плашке;
- прогресс-бар или визуальный индикатор, если он предусмотрен исходным контентом;
- спокойная тёмная поверхность и золотые иконки.

Не добавлять интерактивный прогресс или значения, которых нет в предоставленном тексте.

#### 6.3.5. Компоновка секций

Дополнительные референсы:

- `Референсы/компоновка блока.jpeg`;
- `Референсы/компоновка блока 2.jpeg`;
- `Референсы/e498167fbf0b60fb9c190643bd553cef.jpg`.

Допускается использовать:

- сочетание крупного изображения и нескольких информационных панелей;
- асимметричную модульную сетку;
- текст поверх изображения с обязательным контрастным overlay;
- крупный вводный блок и более компактные карточки преимуществ;
- чередование светлых по плотности и тёмных по насыщенности поверхностей в рамках общей тёмной темы.

Композиционные референсы не являются требованием буквального копирования. Они задают плотность, иерархию и принцип группировки информации.

Обязательные правила выбора композиции на desktop:

1. Если блок содержит заголовок и небольшой список, макет делится вертикально на две равные части в пропорции `50/50`:
   - в одной половине располагаются заголовок и список;
   - во второй половине располагается изображение;
   - базовый вариант — список слева, изображение справа;
   - секция `How to Download the National Casino App` является утверждённым исключением: сначала располагается строка Android с инструкцией слева и изображением справа, затем строка iOS с изображением слева и инструкцией справа;
   - композицию и оформление брать из референсов компоновки, адаптируя их под значения настоящего ТЗ.
2. Если блок состоит из заголовка и небольшого текста объёмом 2–3 абзаца:
   - изображение заполняет всю площадь блока;
   - поверх изображения используется контрастный затемняющий overlay;
   - текстовый контейнер располагается слева и занимает приблизительно половину ширины блока;
   - основная визуальная композиция будущего изображения располагается справа и не конфликтует с текстом;
   - затемняющий overlay имеет чёрный цвет, остаётся плотным до `35%` ширины и плавно заканчивается к `70%`, у правой границы текстовой зоны;
   - композицию брать из референсов компоновки без изменения исходного текста.
3. Если блок содержит таблицу:
   - таблица располагается отдельной строкой и занимает всю доступную ширину основного контентного контейнера;
   - оформление выбирается по соответствующему референсу таблицы;
   - таблицу нельзя сжимать ради размещения рядом с изображением или другим контентом.
4. В блоках, содержащих таблицы или оформленные списки, обычный текст использует всю доступную ширину своей колонки или основного контентного контейнера без дополнительного ограничения длины строки. Это правило не применяется к блокам, где изображение заполняет всю площадь секции, а текст размещается поверх него.
5. На финальном этапе серые заглушки заменяются локальными тематическими изображениями из `public/content`; размеры, пропорции и скругления блоков при замене не изменяются. Для общего баннера приложения сохраняются оригинальное изображение телефона `public/app/app-bonus-phone.webp` и локальный сгенерированный фон `public/app/app-promo-background-v2.jpg`.
6. Для редакционных блоков Main утверждена единая серия локальных 3D-иллюстраций с двумя референсными персонажами, которые в основном чередуются по ходу страницы. Широкие фоновые изображения выполняются в пропорции `16:9`: смысловая композиция и персонаж располагаются справа, левая половина остаётся тёмной сейф-зоной под текст. Изображение в карточке оценки выполняется в пропорции `1:1` с центральной композицией. В блоке `Die Bewertungskarte` используется женщина с гарнитурой поддержки, а в финальном блоке `Das letzte Wort`, написанном от лица мужчины, — мужчина-расследователь. Блок `Boni und Aktionen`, его внутренние карточки, общие промо-баннеры и блок регистрации этой заменой не затрагиваются.

Общий баннер приложения является самостоятельным блоком и не входит в количество редакционных контентных блоков. На странице Main он располагается сразу после блока `Spielen am Handy`, а на странице App — сразу после блока `Was du davon hast`. Баннер занимает всю ширину контентной области; на desktop CTA-ссылка `Lade die iOS-App herunter` находится слева, изображение телефона — по центру, CTA-ссылка `Lade die Android-App herunter` — справа. Фон `public/app/app-promo-background-v2.jpg` выполнен в тёмной премиальной стилистике промо-баннеров: центральная область остаётся свободной под телефон, а рулетка, фишки, колесо наград и монеты распределены по левому и правому краям. Телефон и ссылки располагаются отдельным верхним слоем поверх фона. Используются существующие элементы приложения без изменения их оформления. На mobile элементы перестраиваются в одну колонку, а высота становится адаптивной для сохранения размеров и читаемости CTA.

На страницах Main и App дополнительно располагаются четыре общих промо-баннера, равномерно распределённые между редакционными контентными блоками. Новая страница Bonuses эти вставные баннеры не использует, поскольку её основная структура уже состоит из бонусных карточек. Баннеры не входят в утверждённое количество редакционных блоков. Каждый баннер занимает всю ширину контента и имеет высоту `350px`. Фон заполняется локальным растровым изображением с основной композицией справа и затемнённой зоной слева. На desktop текстовый контейнер расположен слева с отступом `100px` и центрирован по высоте баннера; на tablet/mobile отступ адаптивно уменьшается. Первая строка имеет менее выраженную типографику и белый цвет `#FFFFFF`. Основное предложение отображается крупным текстом с вертикальным градиентом от `#FFC700` сверху к `#FF8A00` снизу. Под текстом через `20px` располагается CTA `Bonus Holen`.

Распределение баннеров по порядку контента:

- Main — после 4-го, 8-го, 12-го и 16-го редакционных блоков;
- App — после 2-го, 4-го, 6-го и 8-го редакционных блоков.

Общий набор промо-баннеров и текстов:

1. `Einzahlungsbonus` — `100 % bis zu 100 EUR + 100 Freispiele`, фон `public/banners/first-deposit-v2.jpg` с золотым хранилищем и монетами справа.
2. `Zweite Einzahlungsbonus` — `50 % bis zu 200 EUR + 50 Freispiele`, фон `public/banners/second-deposit-v2.jpg` с двумя световыми порталами и сундуками справа.
3. `Highroller-Bonus` — `100 % bis zu 750 EUR`, фон `public/banners/highroller-v2.jpg` с короной, высокой башней фишек и рулеткой справа.
4. `Freitags-Reload-Bonus` — `50 % bis zu 250 EUR + 100 Freispiele`, фон `public/banners/friday-reload-v2.jpg` с часовым механизмом перезагрузки и потоком монет справа.

Все новые промо-фоны имеют размер `2068×760`, единую тёмную премиальную 3D-стилистику и спокойную левую safe-зону под текст без надписей, логотипов и персонажей.

В предложениях с Freispielen используется принудительная двухстрочная композиция: значение процента и максимальной суммы целиком располагается на первой строке, а неразрывный фрагмент `+ … Freispiele` — целиком на второй строке. Баннер `Highroller-Bonus`, не содержащий Freispiele, сохраняет одну строку основного предложения.

Дополнительные правила вертикального ритма:

- первый текстовый элемент или заголовок внутри двухколоночной композиции выравнивается по верхней границе соседнего изображения;
- после пошаговых, маркированных и карточных списков сохраняется явный отступ перед следующим абзацем или подразделом;
- внутренние заголовки карточек и оформленных панелей не получают лишний верхний отступ относительно их внутренней границы.

В оформленных маркированных списках жёлтые стандартные маркеры заменяются локальной иконкой огня `public/icons/fire.svg`. Маркеры отрицательных пунктов в блоке недостатков сохраняют красное оформление.

На tablet и mobile двухколоночные композиции перестраиваются в одну колонку с сохранением исходного порядка текста. Серые заглушки изображений остаются в блоках и адаптируются по ширине без появления горизонтального скролла страницы.

На desktop изображение в двухколоночной композиции растягивается строго по высоте соседнего текстового, списочного или табличного столбца, автоматически уменьшается вместе с ним при уплотнении внутренних отступов и не выступает ниже его видимого содержимого. Нижний внешний отступ последнего элемента текстового столбца не должен увеличивать высоту изображения. После перестроения в одну колонку оно получает самостоятельную минимальную высоту для сохранения композиции.

## 7. Технические требования

### 7.1. Стек

- Next.js;
- TypeScript;
- Tailwind CSS;
- HTML;
- CSS;
- JavaScript в случаях, где он необходим для интерактивности.

### 7.2. Общие требования к реализации

- адаптивная вёрстка для desktop, tablet и mobile;
- переиспользуемые компоненты для общих элементов и повторяющихся блоков;
- семантическая HTML-разметка;
- корректная клавиатурная навигация;
- видимые состояния `hover`, `focus`, `active` и `disabled`, где они применимы;
- оптимизация изображений и шрифтов;
- отсутствие ненужного клиентского JavaScript;
- отсутствие горизонтального скролла у страницы на поддерживаемых разрешениях;
- локальный горизонтальный скролл разрешён только внутри специально обозначенного контейнера широкой таблицы;
- корректное отображение длинных таблиц, списков и текстовых блоков на мобильных устройствах.

### 7.3. Интерактивные элементы

Окончательный список интерактивных элементов будет определён после описания страниц и дизайн-системы.

Предварительно могут использоваться:

- мобильное меню;
- якорная навигация;
- FAQ-аккордеоны;
- переключатели или вкладки;
- адаптивные таблицы;
- интерактивные CTA-элементы.

CTA-ссылки должны присутствовать в предусмотренных дизайном блоках и использовать внешние affiliate URL из раздела 6.2.6. В карточных и промо-компонентах интерактивной является только отдельная CTA-ссылка; сама контентная карточка не кликабельна, кроме отдельно утверждённой карточки `Application` в sidebar.

## 8. SEO и метаданные

Строки `Meta title` и `Meta description` в исходных DOCX являются источником видимого заголовка и описания Hero в соответствии с разделом 5.4. Отдельные правила их дополнительного использования в технических SEO-метаданных при необходимости фиксируются позднее.

Для всех опубликованных маршрутов используются абсолютные canonical URL на основном домене `https://natiocasino-germany.vercel.app`: `/`, `/bonuses`, `/app` и `/responsible-gaming`. Каждая страница содержит собственный набор Open Graph metadata: `og:title`, `og:description`, `og:url`, `og:type=website`, `og:site_name=National Casino Deutschland`, `og:locale=de_DE` и `og:image` с размерами и альтернативным текстом. В качестве `og:image` используется соответствующее локальное Hero-изображение страницы из `public/content`. Breadcrumb JSON-LD использует тот же немецкий origin.

Технические Title используют компактные формулировки в рекомендованном диапазоне 45–60 символов; видимые Hero-заголовки сохраняют утверждённый редакционный текст. Итоговый рейтинг на Main унифицирован в формате `4.4/5` в Hero и экспертном блоке. Footer отображает copyright с текущим годом и идентичностью `National Casino Deutschland`.

По прямому указанию пользователя явные дефекты исходных DOCX исправляются в опубликованном тексте без изменения смысла: значение Key Factors `No public inco` восстановлено как `No public information`, а между склеенными предложениями добавлены пропущенные пробелы. Контентные PNG тяжелее 400 КБ в `public/content`, `public/banners` и `public/app` заменены на WebP высокого качества с сохранением исходных размеров; favicon и небольшие служебные PNG не изменяются.

На маршруте `/sitemap.xml` публикуется XML-карта со всеми четырьмя индексируемыми страницами: `/`, `/bonuses`, `/app` и `/responsible-gaming` на origin `https://natiocasino-germany.vercel.app`. Тег `<lastmod>` не используется. На маршруте `/robots.txt` публикуются правила `User-agent: *`, `Allow: /` и абсолютная ссылка `Sitemap: https://natiocasino-germany.vercel.app/sitemap.xml`.

Favicon сайта использует локальную неизменённую копию официального PNG-ассета National Casino из динамической конфигурации оригинального сайта. Файл располагается в `app/icon.png` и подключается средствами Metadata API Next.js. Runtime-загрузка favicon с внешнего CDN не используется.

Видимые блоки FAQ на страницах Main и App сопровождаются JSON-LD `FAQPage`; вопросы и ответы в разметке формируются из тех же данных, что видимые аккордеоны. Видимые отзывы на Main сопровождаются отдельными объектами `Review` с теми же заголовком, автором и текстом, что отображаются в карточках. Каждый объект `Review` содержит обязательный `itemReviewed` типа `Organization` с именем `National Casino`, чтобы Google однозначно связывал отзыв с обозреваемым брендом. Рейтинг добавляется только при наличии отдельного источника и видимого значения, агрегированный рейтинг не добавляется, пока он отдельно не отображается рядом с отзывами. Хлебные крошки страниц Bonuses, App и Responsible Gaming сопровождаются JSON-LD `BreadcrumbList` с абсолютными canonical URL. Структурированные данные не должны содержать скрытый редакционный контент или расходиться с видимой страницей.

Также предусмотреть техническую возможность задать:

- уникальные `title` и `description`;
- canonical URL;
- Open Graph metadata;
- иерархию заголовков H1–H3;
- favicon и изображения для социальных сетей;
- структурированные данные, если они будут указаны в финальном ТЗ.

## 9. Адаптивность

Точные контрольные точки будут определены дизайн-системой.

Необходимо обеспечить:

- полноценную desktop-версию;
- адаптацию для планшетов;
- полноценную mobile-версию;
- удобное чтение длинного контента;
- доступную навигацию;
- корректное отображение карточек, таблиц и пошаговых инструкций.

## 10. Критерии готовности

Проект считается готовым после того, как:

- реализованы страницы Main, Bonuses и App;
- header и footer используются на всех страницах;
- навигация между страницами работает;
- весь согласованный текст размещён в нужных блоках без самостоятельного дополнения;
- дизайн соответствует утверждённой визуальной системе;
- страницы корректно работают на desktop, tablet и mobile;
- отсутствуют ошибки сборки и TypeScript;
- проверены основные интерактивные состояния;
- нет визуальных переполнений, наложений и горизонтального скролла страницы;
- локальный горизонтальный скролл широкой таблицы, если он предусмотрен её адаптивной версией, не вызывает горизонтальный скролл всей страницы.

## 11. Открытые разделы ТЗ

Следующими этапами необходимо заполнить:

1. Брендовые материалы и дизайн-систему.
2. Header и footer.
3. Детальную структуру страницы App.
4. Адаптивное поведение компонентов.
5. Анимации и интерактивные состояния.
6. Финальные критерии проверки дизайна и контента.
